
CLEANUP
    compact BLAS after creation
    use transfer queue 
    use special final compositing pipeline for Manager output 
    abstract pipeline & render graph creation 

SETUP
    render textured objects

FCPW 
    build my own BVH over the objects
    send it to the GPU 
    query it on the GPU with compute shaders
    optimize that BVH 

Rendering 
    set up RTX BVH and render them with ray tracing lel 
    add light sampling, indirect lighting 
    work on ReSTIR
